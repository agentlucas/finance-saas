[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pnpm install"]

[phases.build]
dependsOn = ["install"]
cmds = [
    "cd apps/api",
    "pnpm prisma generate", 
    "pnpm build"
]

[start]
cmd = "cd apps/api && node dist/main.js"
